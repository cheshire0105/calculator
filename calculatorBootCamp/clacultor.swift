//
//  clacultor.swift
//  calculatorBootCamp
//
//  Created by 김도윤 on 2023/07/19.
//
/*
switch self.current { case .Add: // 더하기 let addResult = AddOperation().CalculatorAdd(firstInput, secondInput)
    self.result = addResult case .Subtract: // 빼기 let subtractResult = SubtractOperation().CalculatorSubtract(firstValue: firstInput, secondValue: secondInput)
    self.result = subtractResult
case .Multiple: // 곱하기 let multipleResult = MultiplyOperation().CalculatorMultiply(firstValue: firstInput, secondValue: secondInput) self.result = multipleResult
case .Divide: // 나누기 let divideResult = DivideOperation().CalculatorDivide(firstValue: firstInput, secondValue: secondInput) if secondInput != 0 { self.result = divideResult } else { NoDivideAlert() } default: break }
    
    enum Operation { case Add // 더하기 case Subtract // 빼기 case Multiple // 곱하기 case Divide // 빼기 case None }
    }
*/
